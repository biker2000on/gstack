# Skill catalog

All installed entries use `gstack-<skill>`. Host UIs with display metadata render
the same entry as `gstack: <skill>`.

## Planning and product

`autoplan`, `office-hours`, `spec`, `plan-ceo-review`, `plan-design-review`,
`plan-devex-review`, `plan-eng-review`, and `plan-tune`.

## Review and implementation

`review`, `investigate`, `careful`, `guard`, `health`, `cso`, `devex-review`,
`design-consultation`, `design-html`, `design-review`, and `design-shotgun`.

## QA, browser, and artifacts

`qa`, `qa-only`, `browse`, `open-gstack-browser`, `scrape`, `diagram`,
`make-pdf`, `document-generate`, and `document-release`.

## Delivery and operations

`ship`, `land-and-deploy`, `landing-report`, `canary`, `setup-deploy`,
`setup-browser-cookies`, `benchmark`, and `benchmark-models`.

## Context and collaboration

`context-save`, `context-restore`, `learn`, `retro`, `pair-agent`, `skillify`,
`freeze`, `unfreeze`, `codex`, `claude`, and `gstack-upgrade`.

The repository root skill provides catalog and routing help. Consult each generated
`SKILL.md` for its exact triggers and workflow.
